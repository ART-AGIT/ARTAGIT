<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.artagit.web.dao.RoleDao"> 
	<select id="getMemberByUserName" resultType="Role"> <!-- getList() 라는 함수를 구현하기 위해 사용되는 select문 이라는 의미. -->
		select r.id, r.name
			from Member m 
			join Role r
			on m.roleId = r.id where loginId = #{username}

	</select>
	<select id="getCorpByUserName" resultType="Role"> <!-- getList() 라는 함수를 구현하기 위해 사용되는 select문 이라는 의미. -->
			select r.id, r.name
				from Corporate c
				join Role r
				on c.roleId = r.id where loginId = #{username};
	</select>
</mapper>